var axios = require("axios");
var models = require("../models");

module.exports = {
    findAll: function(req, res {

    })
}

// query google api and only return books we do not have
// Query api and create our result object 
// Query db and determine wether that book is already in the db
// send response to the client